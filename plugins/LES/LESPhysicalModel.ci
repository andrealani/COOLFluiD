namespace COOLFluiD {

    namespace LES {

//////////////////////////////////////////////////////////////////////////////

template <int DIM>
LESPhysicalModel<DIM>::LESPhysicalModel(const std::string& name)
  : Physics::NavierStokes::NavierStokesPhysicalModel<DIM>(name)
{
}

//////////////////////////////////////////////////////////////////////////////

template <int DIM>
LESPhysicalModel<DIM>::~LESPhysicalModel()
{
}

//////////////////////////////////////////////////////////////////////////////

template <int DIM>
void LESPhysicalModel<DIM>::configure ( Config::ConfigArgs& args )
{
  Physics::NavierStokes::NavierStokesPhysicalModel<DIM>::configure(args);
}

//////////////////////////////////////////////////////////////////////////////

    } // namespace LES

} // namespace COOLFluiD

//////////////////////////////////////////////////////////////////////////////

